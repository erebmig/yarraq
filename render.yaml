services:
  - type: web
    name: ytdl-app
    env: python
    buildCommand: |
      apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
